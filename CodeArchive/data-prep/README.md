# Archived data-preparation notebooks

Archived 2026-08-13 for possible later deletion. These notebooks are not run by
the active `MAIN.R` workflow.

`Abbreviations.ipynb` generated
`DataInput/Updated_Yan-Zheng-Compustat-Vars.xlsx`. Keep that tracked workbook in
its active location: `0_Environment.R` reads it through `import_docs()`, which
feeds the chapter-8 theme exhibits.
